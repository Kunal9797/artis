{
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "generate-templates": "cd tools && ts-node scripts/create-consumption-template.ts && ts-node scripts/create-purchase-template.ts && ts-node scripts/create-inventory-template.ts"
  },
  "dependencies": {
    "@types/node": "^22.10.2",
    "lodash": "^4.17.21",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "json-loader": "^0.5.7",
    "react-app-rewired": "^2.2.1"
  }
}
